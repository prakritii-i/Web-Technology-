function square(){
let n=document.getElementById("number").value;
let square=n*n;
document.getElementById("square").innerText = "The square of " + n + " is " + square;
}
function average(){
	var arr=[];
	 for (var i = 0; i < 5; i++) {
        arr.push(parseFloat(document.getElementById("arr" + (i + 1)).value));
    }
	for (var i = 0; i <= 5; i++) {
		sum=sum+a[i];
		}
		var avg=document.write("Average of the five numbers is :"+avg)
	}